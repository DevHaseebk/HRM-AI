# Admin UI only — API secrets live in backend/.env

# Public app URL (links in UI if needed)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Must match backend JWT_SECRET (page middleware verifies hrm_session)
JWT_SECRET=

# Express backend — Next rewrites /api/* here
BACKEND_URL=http://localhost:4000
